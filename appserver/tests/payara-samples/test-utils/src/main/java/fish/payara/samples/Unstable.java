package fish.payara.samples;

/**
 * JUnit 4 unstable category marker. Tests in this category should be enabled only for maven
 * "unstable" profile, e.g. by setting excludedGroups property to "fish.payara.samples.Unstable" by
 * default and setting it to empty value in the "unstable" profile
 */
public interface Unstable {

}
